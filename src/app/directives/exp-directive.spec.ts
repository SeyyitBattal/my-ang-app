import { ExpDirective } from './exp-directive';

describe('ExpDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpDirective();
    expect(directive).toBeTruthy();
  });
});
